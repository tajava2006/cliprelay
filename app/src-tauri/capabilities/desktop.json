{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop 전용 권한 (자동 업데이트 + 재시작)",
  "platforms": ["macOS", "linux", "windows"],
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:default",
    "process:allow-restart"
  ]
}
